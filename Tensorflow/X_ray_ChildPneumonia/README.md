# Pediatric Pneumonia Detection Project (v3)

一个完整的深度学习项目模板，适用于儿童胸部X光肺炎检测的科研与实验工作。
该模板支持数据加载、模型训练、可解释性分析、结果可视化及表格输出。

---

## 📁 项目结构说明

```cmd
pediatric_pneumonia_detection/
│
├── data/
│   ├── raw/                    # 原始数据
│   ├── processed/              # 预处理后的数据
│   └── splits/                 # 数据划分文件（train/val/test.txt）
│
├── models/
│   ├── checkpoints/            # 训练中间模型
│   ├── final/                  # 最优模型文件
│   └── export/                 # 转换为TFLite/ONNX的模型
│
├── outputs/
│   ├── figures/                # 可视化图像输出 (训练曲线, ROC, Grad-CAM)
│   ├── metrics/                # 模型性能指标CSV/Excel
│   └── reports/                # 自动生成的实验总结报告
│
├── src/
│   ├── config/                 # 配置模块
│   ├── data/                   # 数据加载与增强
│   ├── models/                 # 模型定义 (ViT, DenseNet+CBAM)
│   ├── training/               # 训练逻辑、回调与日志
│   ├── evaluation/             # 模型评估与图表生成
│   ├── explainability/         # 可解释性模块 (Grad-CAM等)
│   ├── utils/                  # 工具函数 (日志, GPU, 文件)
│   └── export/                 # 模型导出 (TFLite/ONNX)
│
├── docs/                       # 文档与论文草稿
├── logs/                       # TensorBoard与训练日志
├── notebooks/                  # 实验探索
├── requirements.txt
├── README.md
└── run_train.py                # 顶层运行脚本入口

```

---

## 🚀 快速开始

```bash
# 安装依赖
pip install -r requirements.txt

# 启动训练
python run_train.py
```

训练完成后，结果将自动保存至 `outputs/` 文件夹：

- 训练曲线图：`outputs/figures/training_curves.png`
- 模型性能表格：`outputs/metrics/metrics.csv`
- 可解释性图像（Grad-CAM）：`outputs/figures/gradcam/`
